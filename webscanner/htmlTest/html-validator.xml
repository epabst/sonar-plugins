<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <groupId>html</groupId>
  <artifactId>${artifact}</artifactId>
  <version>1.0</version>

  <name>${artifact}</name>
  <packaging>pom</packaging>

  <properties>
    <sonar.html.sourceDirectory>target/spider/${artifact}</sonar.html.sourceDirectory>
    <sonar.html.website>http://www.${artifact}</sonar.html.website>
  </properties>

  <profiles>
    <profile>
      <id>html</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <properties>
        <sonar.branch>html</sonar.branch>
        <sonar.language>html</sonar.language>
        <sonar.dynamicAnalysis>false</sonar.dynamicAnalysis>
        <sonar.html.fileExtensions>html</sonar.html.fileExtensions>
        <sonar.toetstool.url>http://api.toetstool.nl/</sonar.toetstool.url>
      </properties>
    </profile>

    <!-- Sonar server -->
    <profile>
      <id>postgresql</id>
      <activation>
        <activeByDefault>true</activeByDefault>
      </activation>
      <properties>
        <sonar.jdbc.url>jdbc:postgresql://localhost/sonar</sonar.jdbc.url>
        <sonar.jdbc.driver>org.postgresql.Driver</sonar.jdbc.driver>
        <sonar.jdbc.username>sonar</sonar.jdbc.username>
        <sonar.jdbc.password>sonar</sonar.jdbc.password>
        <sonar.host.url>http://localhost:9000</sonar.host.url>
      </properties>
    </profile>
  </profiles>

</project>
